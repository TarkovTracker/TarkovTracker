# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

# This file can check out insecure code from a PR - as such it explicitly
# is linked to an environment which should always require a review before running

name: Deploy to Firebase Hosting on PR
on: 
  pull_request_target:
    types: [opened, synchronize]
    paths:
      - 'src/**'
      - '*.js'
      - '*.json'
      - 'tarkovdata'
      - 'tarkovdata/*'
      - 'tarkovdata/**'

jobs:
  build_and_preview:
    name: Build and Preview
    runs-on: ubuntu-latest
    environment: testing
    steps:
      - name: Check out source code
        uses: actions/checkout@v2
        with:
          submodules: recursive
          ref: ${{ github.event.pull_request.head.sha }}
          
      - name: Set up build environment
        run: npm install

      - name: Deploy to Firebase Hosting Preview Channel
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_TARKOVTRACKER_TESTING }}'
          projectId: tarkovtracker-testing
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels